public class GamblingPlayer extends Player implements Gambler{
	String name;
	double balance;

	Player(String name, double balance){
		super(name);
		this.balance = balance;
	}

	public double placeBet(){
		//ask for bet
		//check that bet is less than or equal to balance
		balance -= bet
		return bet;
	}

	public double getWinnings(double winnings){
		balance += winnings;
		return winnings;
	}

	//setters and getters
}